'use strict';

{
	const array = [1, 2, 3, -4];
	const result = array.includes(3);
	console.dir({result});
}

{
	const  array = ['b', 'c'];
	const result = array.includes('a');
	console.dir({result});
}